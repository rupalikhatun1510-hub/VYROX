services:
  db:
    image: postgres:16
    container_name: vyrox_db
    environment:
      POSTGRES_USER: vyrox
      POSTGRES_PASSWORD: vyrox_dev_password
      POSTGRES_DB: vyrox
    ports:
      - "5432:5432"
    volumes:
      - vyrox_pgdata:/var/lib/postgresql/data:Z

volumes:
  vyrox_pgdata:
